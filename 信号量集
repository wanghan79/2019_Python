Swait(S1,t1,d1,…,Sn,tn,dn)
{  while (TRUE){
if (S1≥t1 && … && Sn≥tn )
   for (i=1;i<=n;i++) {  
   Si:=Si-di;
   break;}
else { Place the executing process in the waiting queue of the first Si with Si<ti 
       and set its program counter to the beginning of the Swait Operation. }
   }
}

Ssignal(Si,di,…,Sn,dn)
{  while (TRUE){
   for (i=1;i<=n;i++) {
   Si:=Si+di;
   Remove all the process waiting in the queue associated with Si into the ready queue}
   }
}
